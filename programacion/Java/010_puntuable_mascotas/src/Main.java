import static utils.Funciones.menu;

import java.util.ArrayList;
import java.util.Iterator;
import classes.ListaAnimales;

// El leer el tenim en el paquet de funcions
//import utils.Leer;

public class Main {
    public static void main(String[] args) {
        /* menu(); */

        /* ArrayList<Integer> test = new ArrayList<>();
        test.add(1);
        test.add(2);
        test.add(3);
        test.add(4);
        test.add(5); */

        /* for (int num : test) {
            System.out.println(num);
            if (num == 2) {
                test.remove(num);
            }
        }

        for (int num : test) {
            System.out.println(num);
        } */

        /* Iterator<Integer> it = test.iterator();
        while(it.hasNext()) {
            Integer i = it.next();
            if(i == 2) {
                it.remove();
            }
            System.out.println(test);
        } */
    }
}